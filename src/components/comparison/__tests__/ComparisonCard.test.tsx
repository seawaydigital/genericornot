// ComparisonCard has been replaced by ProductCard.
// Tests for ProductCard are in ProductCard.test.tsx.
// This file is kept as a placeholder to avoid breaking the test suite.
import { describe, it } from "vitest";

describe("ComparisonCard (replaced by ProductCard)", () => {
  it("is covered by ProductCard.test.tsx", () => {
    // See ProductCard.test.tsx for the replacement component tests.
  });
});
